name: ConnectX Runner Probe

on:
  push:
    branches:
      - feat/connectx-negocio-os-core-v01
  workflow_dispatch:

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      - name: Runner alive
        run: echo "ConnectX runner OK"
