{
  "function_declarations": [
    {
      "name": "read_file",
      "description": "Lee el contenido de un archivo en el repositorio",
      "parameters": {
        "type": "OBJECT",
        "properties": {
          "path": { "type": "STRING", "description": "Ruta relativa al archivo dentro del repo" }
        },
        "required": ["path"]
      }
    },
    {
      "name": "write_file",
      "description": "Escribe o actualiza un archivo en la rama del agente",
      "parameters": {
        "type": "OBJECT",
        "properties": {
          "path": { "type": "STRING", "description": "Ruta relativa del archivo" },
          "content": { "type": "STRING", "description": "Contenido completo del archivo" }
        },
        "required": ["path", "content"]
      }
    },
    {
      "name": "list_files",
      "description": "Lista archivos del repositorio",
      "parameters": {
        "type": "OBJECT",
        "properties": {
          "sub_path": { "type": "STRING", "description": "Subdirectorio opcional" }
        }
      }
    },
    {
      "name": "commit",
      "description": "Hace commit de los cambios pendientes",
      "parameters": {
        "type": "OBJECT",
        "properties": {
          "message": { "type": "STRING", "description": "Mensaje del commit" }
        },
        "required": ["message"]
      }
    },
    {
      "name": "push",
      "description": "Push de la rama del agente al remoto",
      "parameters": { "type": "OBJECT", "properties": {} }
    },
    {
      "name": "git_log",
      "description": "Historial de commits recientes",
      "parameters": {
        "type": "OBJECT",
        "properties": {
          "limit": { "type": "NUMBER", "description": "Número de commits (default 10)" }
        }
      }
    },
    {
      "name": "parlamento_read",
      "description": "Lee la bitácora compartida del Parlamento",
      "parameters": { "type": "OBJECT", "properties": {} }
    },
    {
      "name": "parlamento_write",
      "description": "Agrega tu entrada al Parlamento de las Sillas",
      "parameters": {
        "type": "OBJECT",
        "properties": {
          "files_modified": { "type": "ARRAY", "items": { "type": "STRING" } },
          "what_was_done": { "type": "ARRAY", "items": { "type": "STRING" } },
          "pending_for_other": { "type": "ARRAY", "items": { "type": "STRING" } },
          "notes": { "type": "STRING" },
          "last_commit": { "type": "STRING" }
        },
        "required": ["files_modified", "what_was_done", "pending_for_other", "notes", "last_commit"]
      }
    },
    {
      "name": "parlamento_status",
      "description": "Estado del Parlamento: quién trabajó, qué está pendiente",
      "parameters": { "type": "OBJECT", "properties": {} }
    }
  ]
}
