[
  {
    "name": "read_file",
    "description": "Lee el contenido de un archivo en el repositorio",
    "input_schema": {
      "type": "object",
      "properties": {
        "path": { "type": "string", "description": "Ruta relativa al archivo dentro del repo" }
      },
      "required": ["path"]
    }
  },
  {
    "name": "write_file",
    "description": "Escribe o actualiza un archivo en la rama del agente",
    "input_schema": {
      "type": "object",
      "properties": {
        "path": { "type": "string", "description": "Ruta relativa del archivo" },
        "content": { "type": "string", "description": "Contenido completo del archivo" }
      },
      "required": ["path", "content"]
    }
  },
  {
    "name": "list_files",
    "description": "Lista archivos del repositorio, opcionalmente filtrando por subdirectorio",
    "input_schema": {
      "type": "object",
      "properties": {
        "sub_path": { "type": "string", "description": "Subdirectorio opcional para filtrar" }
      }
    }
  },
  {
    "name": "commit",
    "description": "Hace commit de los cambios pendientes en la rama del agente",
    "input_schema": {
      "type": "object",
      "properties": {
        "message": { "type": "string", "description": "Mensaje del commit" }
      },
      "required": ["message"]
    }
  },
  {
    "name": "push",
    "description": "Hace push de la rama del agente al remoto",
    "input_schema": { "type": "object", "properties": {} }
  },
  {
    "name": "git_log",
    "description": "Muestra el historial de commits recientes",
    "input_schema": {
      "type": "object",
      "properties": {
        "limit": { "type": "number", "description": "Número de commits a mostrar (default 10)" }
      }
    }
  },
  {
    "name": "parlamento_read",
    "description": "Lee la bitácora compartida del Parlamento de las Sillas",
    "input_schema": { "type": "object", "properties": {} }
  },
  {
    "name": "parlamento_write",
    "description": "Agrega tu entrada de trabajo al Parlamento de las Sillas",
    "input_schema": {
      "type": "object",
      "properties": {
        "files_modified": { "type": "array", "items": { "type": "string" }, "description": "Archivos que modificaste" },
        "what_was_done": { "type": "array", "items": { "type": "string" }, "description": "Lista de cambios realizados" },
        "pending_for_other": { "type": "array", "items": { "type": "string" }, "description": "Tareas pendientes para la otra IA" },
        "notes": { "type": "string", "description": "Notas adicionales" },
        "last_commit": { "type": "string", "description": "Hash del último commit realizado" }
      },
      "required": ["files_modified", "what_was_done", "pending_for_other", "notes", "last_commit"]
    }
  },
  {
    "name": "parlamento_status",
    "description": "Obtiene el estado del Parlamento: quién trabajó, qué está pendiente",
    "input_schema": { "type": "object", "properties": {} }
  }
]
